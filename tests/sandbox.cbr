
f := open("C:\\dev\\cebra\\tests\\test.py")

read_by_line(f)

//rewind(f)

read_by_line(f)

close(f)


read_by_line :: (f: File) -> () {
    line := 1
    while !eof(f) {
        print("[line " + line as string + "]    " + read_line(f)) 
        line = line + 1
    }
}
