
    //f: File = open("C:\\dev\\cebra\\tests\\test.py")
    //s := read_all(f)
    //close(f)
    //print(s)
    f := open("C:\\dev\\cebra\\tests\\test.py")
    eof := false
    line := 1
    while !eof {
        s: string, eof = read_line(f)
        print("[line " + line as string + "]    " + s) 
        line = line + 1
    }
    close(f)
