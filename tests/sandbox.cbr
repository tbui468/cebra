Dog :: struct {
    name := "Mittens"
    bark: (Dog) -> = nil
}

d := Dog()
d.bark = bark

bark :: (d: Dog) -> {
    print(d.name)
}

d.bark(d)

d2 := Dog()
d2.name = "Bubbles"
d2.bark = bark

d2.bark(d2)
d2.bark(d)
